# 環境變數 (非常重要！包含你的 API Key 絕對不能推上 GitHub)
.env

# Node.js 依賴套件 (體積很大，且別人 pull 下來後自己 npm install 即可)
node_modules/
package-lock.json
package.json

# 執行腳本產生的截圖與暫存檔 (避免把測試紀錄一起 push 上去)
screenshot.png
gemini_suggestion.md

# Mac 系統自動產生的隱藏檔案
.DS_Store

# npm 執行錯誤時產生的日誌檔
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 編輯器相關的設定檔 (可選，避免你的 VS Code 設定干擾到其他協作者)
.vscode/
